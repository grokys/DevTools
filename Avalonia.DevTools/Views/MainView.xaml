<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:Avalonia.DevTools.Views">
  <Grid RowDefinitions="Auto,Auto,2*,Auto,*,Auto">
    <TabStrip SelectedIndex="{Binding SelectedTab, Mode=TwoWay}">
      <TabStripItem Content="Logical Tree"/>
      <TabStripItem Content="Visual Tree"/>
    </TabStrip>

    <Rectangle Grid.Row="1" Height="4" Fill="Silver"/>
    
    <ContentControl Grid.Row="2" Content="{Binding Content}"/>

    <GridSplitter Grid.Row="3" Background="Silver" Height="4"/>
    
    <views:ConsoleView Grid.Row="4" DataContext="{Binding Console}"/>
    
    <StackPanel Grid.Row="5" Gap="4" Orientation="Horizontal">
      <TextBlock>Hold Ctrl+Shift over a control to inspect.</TextBlock>
      <Separator Width="8"/>
      <TextBlock>Focused:</TextBlock>
      <TextBlock Text="{Binding FocusedControl}"/>
      <Separator Width="8"/>
      <TextBlock>Pointer Over:</TextBlock>
      <TextBlock Text="{Binding PointerOverElement}"/>
    </StackPanel>
  </Grid>
</UserControl>